# HeadOfContext — Claude Code

@AGENTS.md

`AGENTS.md` carries the rules every contributor follows: mission and non-goals, the frozen stack,
invariants I1–I5, the security rules, the method (ADR before code, tests before implementation,
adversarial self-review, real services in integration tests, Conventional Commits), the commands
and the definition of done. Read it first; when in doubt, re-read it before acting.

Design rationale: `docs/adr/`. Threat model: `docs/threat-model.md`. Free / paid boundary:
`docs/boundary.md`. User documentation: the `documentation` repository.
